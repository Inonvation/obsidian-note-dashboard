---
description: 同步中英文 README 内容
usage: sync-docs
examples:
  - sync-docs
---

# 文档同步命令

加载 `docs-sync` skill 执行文档同步。

## 执行流程

1. 加载 `docs-sync` skill
2. 按 skill 定义的流程执行
3. 输出 skill 定义的输出格式

## 示例

```
# 执行文档同步
sync-docs
```

## 输出

同步报告包含：
- 差异项列表
- 同步操作记录
- 验证结果