# 笔记看板优化方案

> 基于 v3 平滑渲染版（1812行）的全面分析

## 一、性能优化（按优先级排序）

### P0：词数统计缓存（预计提速 60-80%）

**现状**：每次打开看板都通过 `dv.io.load()` 逐文件读取内容计算词数，200 篇笔记 = 200 次 IO。

**方案**：localStorage 缓存词数，仅重算变化文件。

```javascript
// 替换 WordCache.load() 逻辑
const WordCache = {
    _cache: null,
    CACHE_KEY: 'nd-word-cache',

    _fileKey(p) {
        return `${p.file.path}:${p.file.size}:${p.file.mtime||0}`;
    },

    load(pages) {
        // 1. 读取缓存
        let cached = {};
        try { cached = JSON.parse(localStorage.getItem(this.CACHE_KEY) || '{}'); } catch {}

        // 2. 检查哪些文件需要重算
        const wordByPath = new Map();
        let totalWords = 0;
        const toLoad = [];

        for (const p of pages.values) {
            const fk = this._fileKey(p);
            if (cached[fk] !== undefined) {
                wordByPath.set(p.file.path, cached[fk]);
                totalWords += cached[fk];
            } else {
                toLoad.push(p);
            }
        }

        // 3. 大笔记库走估算
        if (toLoad.length > cfg.estThreshold) {
            for (const p of toLoad) {
                const est = Math.round((p.file.size || 0) / cfg.estCoeff);
                wordByPath.set(p.file.path, est);
                totalWords += est;
                cached[this._fileKey(p)] = est;
            }
            localStorage.setItem(this.CACHE_KEY, JSON.stringify(cached));
            return { wordByPath, totalWords };
        }

        // 4. 只加载变化的文件
        if (toLoad.length === 0) {
            return { wordByPath, totalWords };
        }

        return Promise.all(toLoad.map(p => dv.io.load(p.file.path).catch(() => '')))
            .then(raws => {
                for (let i = 0; i < toLoad.length; i++) {
                    const wc = this._wordCount(raws[i] || '');
                    wordByPath.set(toLoad[i].file.path, wc);
                    totalWords += wc;
                    cached[this._fileKey(toLoad[i])] = wc;
                }
                // 清理旧缓存（保留最近 500 条）
                const keys = Object.keys(cached);
                if (keys.length > 500) {
                    const sorted = keys.sort(); // mtime 在 key 中，排序即按时间
                    for (let i = 0; i < sorted.length - 500; i++) delete cached[sorted[i]];
                }
                localStorage.setItem(this.CACHE_KEY, JSON.stringify(cached));
                return { wordByPath, totalWords };
            });
    },

    _wordCount(raw) {
        // ... 保持现有逻辑不变
    }
};
```

**预期效果**：首次加载后，后续打开仅需重算修改过的文件，IO 减少 90%+。

---

### P1：减少 dv.pages() 查询

**现状**：
- 659 行：`dv.pages('')` 加载全部
- 800 行：`allPages.file.tasks` 查询待办
- 1395 行：`allPages.file.tasks.where(t=>t.completed)` 再查已完成

**方案**：合并任务查询。

```javascript
// 一次性获取所有任务
const allTasks = allPages.file.tasks;
const pendingTasks = allTasks.where(t => !t.completed);
const completedTasks = allTasks.where(t => t.completed);
const totalDone = completedTasks.length;
```

---

### P2：DOM 更新优化

**现状**：每次 re-render 都 `root.innerHTML = H.join('')`，完全重建 DOM。

**方案**：对于 re-render（已在 preview 模式），只更新数据变化的部分。

```javascript
// 标记各 section 的 ID，re-render 时只更新数据区
const sections = {
    stats: 'nd-stats-section',
    chart: 'nd-chart-section',
    folder: 'nd-folder-section',
    task: 'nd-task-container'
};

// re-render 时：
if (isReRender) {
    // 只更新统计数字，不重建整个 DOM
    updateStatsValues(allPages, totalWords, ...);
    return;
}
```

**注意**：这个改动较大，建议作为 v4 的优化方向。

---

### P3：热力图优化

**现状**：`generateHeatmapHTML()` 生成 7 x 54 = 378 个 div。

**方案**：用 Canvas 绘制热力图，DOM 节点从 378 降到 1。

```javascript
function generateHeatmapCanvas() {
    const canvas = document.createElement('canvas');
    const W = cfg.heatWeeks;
    canvas.width = LW + W * (S + G) + 12;
    canvas.height = PT + MH + MB + 7 * (S + G);
    const ctx = canvas.getContext('2d');

    // 绘制月份标签
    ctx.font = '9px sans-serif';
    ctx.fillStyle = 'var(--text-muted)'; // 需要从 CSS 变量获取实际颜色
    for (const [col, label] of mMap) {
        ctx.fillText(label, LW + col * (S + G), PT);
    }

    // 绘制格子
    for (let r = 0; r < 7; r++) {
        for (let c = 0; c < W; c++) {
            const d = new Date(start);
            d.setDate(d.getDate() + c * 7 + r);
            const k = `${d.getFullYear()}-${C.pad(d.getMonth()+1)}-${C.pad(d.getDate())}`;
            const words = dateWords.get(k) || 0;
            const lv = level(words);
            ctx.fillStyle = C.clr(lv);
            ctx.beginPath();
            ctx.roundRect(LW + c * (S + G), PT + MH + MB + r * (S + G), S, S, R);
            ctx.fill();
        }
    }

    return canvas;
}
```

**权衡**：Canvas 方案失去 tooltip 和悬停效果，需要额外实现。建议保持 div 方案，但用 `content-visibility: auto` 优化渲染。

---

## 二、代码质量优化

### 2.1 提取 analyzeTask 缓存

**现状**：`analyzeTask()` 在 `renderTaskRow()`、`priBadge()`、`dueBadge()` 中被重复调用，同一任务最多调用 3 次。

**方案**：在排序前一次性分析所有任务。

```javascript
// 在 sortedFiles 生成时
sortedFiles.forEach(g => {
    g.analyzed = g.tasks.map(t => ({ task: t, ...analyzeTask(t) }));
    g.analyzed.sort((a, b) => {
        if (a.isUrgent !== b.isUrgent) return a.isUrgent ? -1 : 1;
        if (a.isImportant !== b.isImportant) return a.isImportant ? -1 : 1;
        return 0;
    });
});

// renderTaskRow 接收 analyzed 对象而非原始 task
function renderTaskRow(analyzed, idx) {
    const a = analyzed; // 已分析过
    // ...
}
```

---

### 2.2 简化 Preview 模式强制

**现状**：205-274 行的逻辑包括重试、退避、布局就绪检查，复杂度高。

**方案**：简化为单次尝试 + MutationObserver 监听。

```javascript
function forcePreviewMode() {
    dv.app.workspace.iterateAllLeaves(l => {
        if (l?.view?.file?.path !== C.selfPath) return;
        const st = l.getViewState();
        if (st?.state?.mode !== 'preview') {
            l.setViewState({ ...st, state: { ...st.state, mode: 'preview' } });
        }
    });
}

// 单次尝试
let ndIsPre = false;
dv.app.workspace.iterateAllLeaves(l => {
    if (l?.view?.file?.path === C.selfPath && l.getViewState()?.state?.mode === 'preview') ndIsPre = true;
});

if (!ndIsPre) {
    dv.container.innerHTML = '';
    dv.app.workspace.onLayoutReady(() => forcePreviewMode());
    return;
}
```

---

### 2.3 清理全局变量

**现状**：`window[watchKey]`、`window.__ndRenderedKey`、`window.__ndThemeObs` 污染全局。

**方案**：用单一命名空间。

```javascript
// 统一挂在 window.__nd 下
if (!window.__nd) window.__nd = {};
const nd = window.__nd;

nd.renderedKey = dataKey;
nd.themeObs = new MutationObserver(...);
nd.watchers = new Set();
```

---

## 三、渲染速度优化

### 3.1 CSS Containment

给各卡片添加 `contain` 属性，让浏览器跳过不可见区域的渲染。

```css
.nd-card {
    contain: content;
    content-visibility: auto;
}
```

### 3.2 减少重排

**现状**：`animateFold()` 中 `void el.offsetHeight` 强制同步布局。

**优化**：用 `requestAnimationFrame` 替代。

```javascript
// 替代 void el.offsetHeight
requestAnimationFrame(() => {
    el.style.maxHeight = el.scrollHeight + 'px';
});
```

### 3.3 事件委托优化

**现状**：待办看板的点击事件委托在 `#nd-dash` 上，但 `.closest()` 遍历较深。

**方案**：给待办容器单独委托。

```javascript
taskContainer.addEventListener('click', function(e) {
    // 只在待办区域内冒泡，减少 .closest() 遍历深度
});
```

---

## 四、功能建议

### 4.1 按标签/关键词筛选待办

在待办看板顶栏添加搜索框，实时过滤任务。

### 4.2 热力图点击跳转

点击热力图格子，跳转到对应日期的日记。

### 4.3 文件夹排行趋势

记录历史数据，显示文件夹词数的月度变化趋势。

---

## 五、优化优先级路线图

| 阶段 | 优化项 | 预期收益 | 工作量 |
|------|--------|----------|--------|
| v3.1 | 词数缓存 | 60-80% 加速 | 小 |
| v3.1 | 合并任务查询 | 10-15% 加速 | 小 |
| v3.1 | analyzeTask 缓存 | 代码质量 | 小 |
| v3.2 | CSS Containment | 滚动性能 | 小 |
| v3.2 | 简化 Preview 逻辑 | 代码质量 | 中 |
| v3.3 | DOM 差异更新 | 30-50% 加速 | 大 |
| v4.0 | Canvas 热力图 | 内存优化 | 大 |

---

## 六、测试建议

优化后需要验证：
1. 首次加载时间（Chrome DevTools Performance）
2. Re-render 时间（切换笔记再切回）
3. 内存占用（DevTools Memory）
4. 各功能正常：热力图 tooltip、柱状图切换、待办标记完成、设置面板保存
5. 深色/浅色主题切换
