---
name: docs-sync
description: 中英文文档同步。当用户提到"同步"、"sync"、"文档同步"时触发。
license: MIT
compatibility: opencode
metadata:
  category: workflow
  workflow: docs-sync
  version: 1.0.0
---

# Docs Sync Workflow

## 触发条件

用户提到以下关键词时触发：
- 同步、sync、文档同步、README 同步

## 同步规则

- 中英文内容同步，差异仅限于语言
- 锚点命名：中文 `<a name="feature"></a>` / 英文 `<a name="feature_en"></a>`
- badge 行数保持一致
- 更新日志新版本条目同步添加
- 中文用中文标点，英文用英文标点
- 保持相同 HTML 结构和样式类名，不单方增删段落

## 执行步骤

### Step 1: 读取文件

```
输入：无
处理：
  1. 读取 README.md（中文）
  2. 读取 README_EN.md（英文）
输出：两个文件内容
```

### Step 2: 对比差异

```
输入：两个文件内容
处理：
  1. 逐项对比内容差异
  2. 记录差异项
输出：差异列表
```

### Step 3: 同步内容

```
输入：差异列表
处理：
  1. 根据同步规则同步内容
  2. 确保格式一致
输出：同步后的内容
```

### Step 4: 验证

```
输入：同步后的内容
处理：
  1. 重新对比确认无遗漏
  2. 用 `git diff --stat` 确认变更内容符合预期
输出：验证结果
```

## 输出格式

```
## 文档同步报告

### 差异项
- [差异描述]

### 同步操作
- [操作描述]

### 验证结果
- [验证结果]
```

## 错误处理

| 错误场景 | 处理方式 |
|----------|----------|
| 文件不存在 | 提示用户检查文件路径 |
| 格式不一致 | 提示用户检查格式 |